# Pasos para ejecutar el programa

1. Crear un entorno virtual e instalar las depedencias del archivo requirements.txt
En windows
```
python -m venv .venv
```
En MAC o Linux
```
python3 -m venv .venv
```